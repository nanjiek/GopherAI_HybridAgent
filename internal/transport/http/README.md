# internal/transport/http 设计说明

## 设计定位
该模块是 HTTP 传输装配层，把中间件、路由与 handler 组织成外部 API 面。

## 核心设计思路
1. 传输层坚持薄实现，负责协议转换，不承载业务决策。
2. 将鉴权、恢复、request-id 等横切能力放在中间件，避免重复逻辑。
3. 通过统一响应模型和状态码策略保证 API 一致性。

## 边界与依赖
边界在于 HTTP 协议适配，不直接访问数据库和模型。

## 演进策略
后续可加入 API 版本协商、限流中间件与 OpenAPI 生成。
