# internal/config 设计说明

## 设计定位
该模块提供统一配置入口，目标是把运行时差异与业务逻辑彻底解耦。

## 核心设计思路
1. 采用环境变量优先策略，默认值与 SDD 保持一致，确保本地可直接运行。
2. 配置对象按领域分组，降低跨模块耦合。
3. 读取失败时走保守回退，优先保证服务可启动并可观测。

## 边界与依赖
边界在于仅负责配置读取与结构化，不承担校验编排和业务策略。

## 演进策略
后续可升级为多源配置并增加配置热更新。
