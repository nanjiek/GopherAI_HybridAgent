# internal/security/secret 设计说明

## 设计定位
该模块负责密钥读取与注入，目标是把密钥来源从代码中分离。

## 核心设计思路
1. 优先支持 `*_FILE` 方式读取，适配容器 secret 挂载。
2. 兼容环境变量直读，保证本地开发最小成本。
3. 对业务层暴露统一读取接口，避免在各模块重复处理密钥来源差异。

## 边界与依赖
该模块只做“读取”，不承担密钥生命周期治理（轮转、审批、审计）。

## 演进策略
后续可接入 Vault/KMS 并增加缓存与失败降级策略。
