# internal/rag 设计说明

## 设计定位
该模块定义 Go 与 Python RAG 子系统之间的协议模型。

## 核心设计思路
1. 把跨语言调用协议显式建模，避免弱类型 JSON 造成契约漂移。
2. 请求响应结构与 SDD 保持一致，保障替换实现时上层无感。
3. 协议优先于实现，先确保接口稳定，再迭代底层检索能力。

## 边界与依赖
边界在于协议定义，不做网络调用或检索算法。

## 演进策略
后续可引入 schema 版本字段，支持多版本兼容演进。
