# internal/core/model 设计说明

## 设计定位
该模块定义领域模型与跨层传输对象，是系统语言统一的基础。

## 核心设计思路
1. 把会话、消息、引用、用量等核心概念沉淀为稳定结构，减少语义漂移。
2. 领域模型先于存储模型，避免数据库字段直接污染业务语义。
3. Query 输入输出保持面向用例，方便 handler/service/repo 解耦协作。

## 边界与依赖
边界在于仅提供领域语义，不依赖数据库、HTTP 或第三方 SDK。

## 演进策略
后续可引入更清晰的子域拆分并加入版本兼容标记。
