# internal/obs/logger 设计说明

## 设计定位
该模块统一日志初始化，保证跨模块日志格式、级别和字段策略一致。

## 核心设计思路
1. 采用结构化日志，优先可检索字段而非自由文本。
2. 日志配置与业务解耦，避免每个模块重复构建 logger。
3. 将日志作为排障数据面，与 trace/metric 形成互补。

## 边界与依赖
边界在于仅提供 logger 构建，不负责业务日志内容定义。

## 演进策略
后续可对接集中式日志平台并补充敏感字段脱敏策略。
