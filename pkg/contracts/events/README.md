# pkg/contracts/events 设计说明

## 设计定位
该模块定义异步消息契约，是生产者与消费者之间的稳定边界。

## 核心设计思路
1. 事件字段保持显式版本化，避免隐式升级带来消费失败。
2. 幂等键、请求 ID、追踪 ID 作为一等字段，保障可恢复与可观测。
3. 契约先行，确保多语言多进程都按同一结构通信。

## 边界与依赖
边界在于消息结构定义，不负责队列连接和处理逻辑。

## 演进策略
后续可引入 schema registry 或 JSON schema 自动校验。
