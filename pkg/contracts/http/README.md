# pkg/contracts/http 设计说明

## 设计定位
该模块定义 HTTP API 的稳定契约，目标是降低前后端联调成本。

## 核心设计思路
1. 统一响应包络与数据对象，减少接口风格漂移。
2. 契约结构与业务模型解耦，避免内部变更直接影响外部 API。
3. 通过集中定义 DTO，保证 handler 输出一致。

## 边界与依赖
边界在于协议模型，不负责业务编排和存储。

## 演进策略
后续可加入多版本 DTO 兼容策略。
