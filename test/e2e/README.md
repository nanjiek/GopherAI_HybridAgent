# test/e2e 设计说明

## 设计定位
该模块验证从路由到服务的最小可用闭环，保证主路径不被重构破坏。

## 核心设计思路
1. 采用轻量 smoke test，优先覆盖关键入口与响应契约。
2. 通过 mock 依赖隔离外部系统，保证测试稳定快速。
3. 把 e2e 当作发布前守门，验证基础编排无断链。

## 边界与依赖
边界在于端到端骨架验证，不替代压力测试与真实集成测试。

## 演进策略
后续可补充真实依赖环境下的集成回归套件。
